name: CI
on:
    push:
      branches: [ "lab5" ]

jobs:
    test_job_ubuntu:
      runs-on: ubuntu-latest
      steps:
          - name: Run a one-line script
            run: echo Hello, workflow!
    test_job_windows:
      runs-on: windows-latest
      steps:
          - name: Run a one-line script
            run: echo Hello, workflow!
